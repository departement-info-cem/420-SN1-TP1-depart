# Ce fichier est un module
# On y définit des fonctions mais on ne les appelle pas


def racine_arrondi_dicho(nombre, decimales = 5):
  return 0.0

def racine_arrondi_chiffres(nombre, decimales = 5):
  return 0.0

def racine_arrondi_n(nombre, exposant=2,  decimales = 5):
  """
    Calcule la racine n-ième de `nombre`, puis arrondit le résultat.

    Paramètres:
      - nombre (int/float): Le nombre dont on veut la racine.
      - exposant (int): L'exposant applicable à la racine. La valeur par défaut de 2 calcule la racine carrée, 3 donnerait la racine cubique.
      - decimales (int): Le nombre de décimales après la virgule pour l'arrondi.

    Retourne:
      float: La racine de `nombre` arrondie selon l'exposant choisi.

    Lance une exception dans les cas suivants:
        - si `nombre` est négatif et `exposant` est pair
        - si `exposant` est inférieur ou égal à 0
        - si `decimales` est négatif
  """
  return 0.0

def racine(nombre):
  return 0.0
  	 		  	  	 		  	    				 		 	 		     	
